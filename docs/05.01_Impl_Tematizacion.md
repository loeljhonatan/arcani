Paso 1: Crear la interfaz del Tema (Shared Domain)
Este archivo define el ADN visual que viajará desde el Oráculo (NestJS) hasta el Motor Reactivo (Angular).
Ubicación: libs/shared/domain/src/lib/interfaces/core/niche-theme.interface.ts
typescript
/**
 * ADN Visual de la Sintonía de Frecuencia (Regla 90/7/3)
 */
export interface NicheTheme {
 /**
 * ADN Visual de la Sintonía de Frecuencia (Regla 90/7/3)
 */
export interface NicheTheme {
  // Aura (7%): Color principal del nicho en formato HSL crudo "H S% L%"
  colorPrimary: string;

  // Destello (3%): Color de acento para CTAs y recompensas
  colorAccent: string;

  // Fondo Luz: Color base para la Frecuencia Estelar
  colorLight: string;

  // Fondo Sombra: Color base para la Frecuencia Carbón
  colorDark: string;

  // Flag de Escasez: Activa la Nebulosa Mística si el stock es bajo
  isBazar: boolean;

  // Configuración Wildcard: Inyección de estilos disruptivos (opcional)
  customConfig?: Record<string, any>;

  // --- Atributos de Geometría ARCANI ---
  borderRadius: string; // ej: "20px" o "0.5rem"
  glassBlur: string;    // ej: "12px" o "4px"
}


Usa el código con precaución.
Paso 2: Crear el DTO de actualización (Shared Domain)
Este archivo es vital para el Admin Dashboard y el API Gateway. Asegura que nadie ingrese un color que rompa la interfaz.
Ubicación: libs/shared/domain/src/lib/dtos/nichos/update-theme.dto.ts
typescript
import { IsString, IsBoolean, IsOptional, Matches } from 'class-validator';

/**
 * Validación para la actualización de sintonía desde el Laboratorio
 */
export class UpdateThemeDto {
  // Valida el formato HSL estricto: "0-360 0-100% 0-100%"
  @Matches(/^\d{1,3}\s\d{1,3}%\s\d{1,3}%$/, {
    message: 'El Aura debe seguir el formato H S% L% (ej: "180 100% 50%")',
  })
  @IsString()
  colorPrimary!: string;

  @Matches(/^\d{1,3}\s\d{1,3}%\s\d{1,3}%$/, {
    message: 'El Destello debe seguir el formato H S% L% (ej: "56 100% 50%")',
  })
  @IsString()
  colorAccent!: string;

  @IsString()
  colorLight!: string;

  @IsString()
  colorDark!: string;

  @IsBoolean()
  @IsOptional()
  isBazar?: boolean;
}
Usa el código con precaución.
Paso 3: El Manifiesto (Exportación Pública)
Para que el Monorepo NX reconozca estos archivos, debemos exponerlos en el punto de entrada de la librería.
Ubicación: libs/shared/domain/src/index.ts
typescript
// Agrega estas líneas al final de tu archivo index.ts actual
export * from './lib/interfaces/core/niche-theme.interface';
export * from './lib/dtos/nichos/update-theme.dto';
Usa el código con precaución.
Paso 4: Comandos de Consola (Si no has creado las carpetas)
Si las carpetas interfaces/core o dtos/nichos no existen, créalas manualmente o usa estos comandos desde la raíz (Windows/PowerShell):
powershell
# Crear directorios si no existen
mkdir -Force libs/shared/domain/src/lib/interfaces/core
mkdir -Force libs/shared/domain/src/lib/dtos/nichos

# No es necesario ejecutar npm install para esto, 
# ya que usamos la estructura interna de NX.
Usa el código con precaución.



# ===========================



En este paso, implementaremos el motor que transformará esos datos del servidor en variables CSS reales para que Tailwind 4 y PrimeNG reaccionen instantáneamente.
Paso 5: Implementar el Motor Reactivo (Core Engine)
Vamos a crear el servicio principal que gestiona la sintonía.
Ubicación: libs/core/reactive-engine/src/lib/services/theme-engine.service.ts
typescript
import { Injectable, signal, effect } from '@angular/core';
import { NicheTheme } from '@arcani/shared/domain';

@Injectable({ providedIn: 'root' })
export class ThemeEngineService {
  /**
   * Signal de estado: Almacena la sintonía actual (Frecuencia activa)
   * Se inicia en null hasta que el Oráculo envíe la primera sintonía
   */
  private readonly _activeTheme = signal<NicheTheme | null>(null);
  
  // Exposición pública de solo lectura para componentes
  readonly activeTheme = this._activeTheme.asReadonly();

  constructor() {
    /**
     * EFECTO REACTIVO: El "Sintonizador"
     * Cada vez que el valor de _activeTheme cambia, este bloque se ejecuta
     * automáticamente, inyectando los colores en el ADN del navegador (DOM).
     */
    effect(() => {
      const theme = this._activeTheme();
      if (theme) {
        this.propagateToDom(theme);
      }
    });
  }

  /**
   * Método Maestro: Sintoniza una nueva frecuencia (Nicho o Evento)
   */
  tune(theme: NicheTheme): void {
    this._activeTheme.set(theme);
  }

  /**
   * Inyecta las variables HSL crudas en el Document Object Model
   */
  private propagateToDom(theme: NicheTheme): void {
    const root = document.documentElement;

    // Sintonía 90/7/3: Inyección de variables RAW para Tailwind 4
    root.style.setProperty('--niche-primary-raw', theme.colorPrimary); // Aura (7%)
    root.style.setProperty('--niche-accent-raw', theme.colorAccent);   // Destello (3%)
    
    // Gestión Lumínica (Frecuencias Carbón y Estelar)
    root.style.setProperty('--niche-bg-light', theme.colorLight);
    root.style.setProperty('--niche-bg-dark', theme.colorDark);

    // Protocolo de Inmersión Bazar (Nebulosa Mística)
    if (theme.isBazar) {
      root.setAttribute('data-arcani-state', 'bazar');
    } else {
      root.removeAttribute('data-arcani-state');
    }

    console.log(`[ARCANI ENGINE] Sintonizando frecuencia...`);
  }
}
Usa el código con precaución.
Paso 6: Exportación en el Manifiesto del Core
Debemos asegurar que este servicio sea accesible para todas las aplicaciones del monorepo (client-web, client-mobile, etc.).
Ubicación: libs/core/reactive-engine/src/index.ts
typescript
export * from './lib/services/theme-engine.service';
Usa el código con precaución.
Paso 7: Comandos de Consola (Preparación de directorios)
Si aún no tienes la estructura de carpetas en core/reactive-engine, ejecútalo en tu terminal:
powershell
# Crear el directorio para los servicios del motor
mkdir -Force libs/core/reactive-engine/src/lib/services
Usa el código con precaución.
¿Qué acabamos de lograr?
Acabamos de crear un Motor de Inyección de ADN. Ahora, cualquier aplicación de ARCANI puede simplemente "inyectar" el servicio y llamar a tune(theme). El motor se encargará de que toda la pantalla cambie de color sin necesidad de recargar la página.



# ===================================

¡Excelente ritmo! Ahora vamos a conectar el Motor Reactivo con la piel de la aplicación. En Tailwind CSS v4, la configuración ha cambiado: ya no se usa tanto el archivo tailwind.config.js para variables dinámicas, sino que se integran directamente en el CSS usando capas.
Paso 8: Configurar el ADN Visual en el CSS Global
Vamos a definir cómo las variables que inyecta el ThemeEngineService se transforman en colores reales que tus componentes pueden usar.
Ubicación: apps/client-web/src/styles.css (o el archivo de estilos principal de tu app)
css
@import "tailwindcss";

/* 1. CONFIGURACIÓN DEL SISTEMA (Tailwind 4 @theme) */
@theme {
  /* Mapeo de Colores Reactivos */
  --color-aura: var(--color-aura);
  --color-destello: var(--color-destello);
  --color-aura-glass: var(--color-aura-glass);
  
  /* Fondos 90% */
  --color-fondo-luz: var(--niche-bg-light);
  --color-fondo-sombra: var(--niche-bg-dark);

  /* Efectos Premium */
  --shadow-aura-neon: 0 0 25px var(--color-aura-glow);
  --shadow-destello-hit: 0 0 40px hsl(var(--niche-accent-raw) / 0.6);

  /* Tipografías de ARCANI */
  --font-maestra: "Syncopate", sans-serif;
  --font-tecnica: "Rajdhani", sans-serif;
}

/* 2. VARIABLES DE ESTADO Y PROCESAMIENTO (Capa Base) */
@layer base {
  :root {
    /* ADN Maestro por defecto (Cian/Amarillo) */
    --niche-primary-raw: 180 100% 50%;
    --niche-accent-raw: 56 100% 50%;
    --niche-bg-light: #ffffff;
    --niche-bg-dark: #0a0a0a;

    /* Procesadores HSL (Cálculos dinámicos) */
    --color-aura: hsl(var(--niche-primary-raw));
    --color-destello: hsl(var(--niche-accent-raw));
    --color-aura-glass: hsl(var(--niche-primary-raw) / 0.12);
    --color-aura-glow: hsl(var(--niche-primary-raw) / 0.35);
  }

  body {
    /* Regla del 90% */
    background-color: light-dark(var(--niche-bg-light), var(--niche-bg-dark));
    color: light-dark(#1a1a1a, #ffffff);
    font-family: var(--font-tecnica);
    
    /* Transición de Sintonía (Fricción Positiva) */
    transition: background-color 0.8s cubic-bezier(0.4, 0, 0.2, 1),
                color 0.4s ease;
    min-height: 100vh;
    overflow-x: hidden;
  }
}

/* 3. COMPONENTES Y ESTADOS ESPECIALES (Capa Components) */
@layer components {
  /* Protocolo Bazar (Nebulosa Mística) */
  [data-arcani-state="bazar"] {
    /* Desaturación sutil para dar sensación de 'reliquia' o 'antigüedad' */
    filter: grayscale(0.3) contrast(1.05);
    transition: filter 1.2s ease-in-out;
  }

  /* El Aura de los componentes UI */
  .arcani-card {
    background: light-dark(rgba(255,255,255,0.7), rgba(0,0,0,0.4));
    border: 1px solid var(--color-aura-glass);
    backdrop-filter: blur(12px);
    transition: all 0.3s ease;
  }

  .arcani-card:hover {
    border-color: var(--color-aura);
    box-shadow: var(--shadow-aura-neon);
  }

  /* Botón de Destello (3%) */
  .btn-destello {
    background-color: var(--color-destello);
    color: #000;
    font-family: var(--font-maestra);
    font-weight: bold;
    text-transform: uppercase;
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .btn-destello:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-destello-hit);
  }
}
Usa el código con precaución.



Paso 09: Prueba de Sintonía (Componente de prueba)
Solo para verificar que todo está conectado, puedes usar esto en cualquier HTML de tu aplicación:
html
<!-- Prueba de Sintonía 90/7/3 -->
<div class="p-8 border border-aura bg-aura-glass shadow-aura-neon rounded-xl">
  <h1 class="text-aura font-bold">Portal Sintonizado</h1>
  <button class="bg-destello text-black px-4 py-2 rounded-md hover:scale-105 transition-transform">
    Acción de Destello
  </button>
</div>
Usa el código con precaución.




# ===========================================================================


¡Excelente! La imagen confirma que el ADN Maestro (Cian Core y Amarillo Volt) ya está fluyendo correctamente a través de tus variables de CSS. Ese botón de "Acción de Destello" es la prueba de que el mapeo de Tailwind 4 está operativo.
Ahora vamos al Oráculo (Backend). Necesitamos que cada vez que el cliente pida datos de un nicho, el servidor adjunte automáticamente la configuración de sintonía.

Paso 11: Implementar el Interceptor Temático (NestJS)
Este interceptor capturará la respuesta del servidor y, si contiene datos de un nicho, inyectará la Regla 90/7/3.
Ubicación: apps/api-gateway/src/app/common/interceptors/reactive-theme.interceptor.ts
typescript
import { Injectable, NestInterceptor, ExecutionContext, CallHandler } from '@nestjs/common';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';
import { NicheTheme } from '@arcani/shared/domain';

@Injectable()
export class ReactiveThemeInterceptor implements NestInterceptor {
  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {
    return next.handle().pipe(
      map((data) => {
        // Si la respuesta es un nicho, le inyectamos su ADN visual
        if (data && data.slug) {
          return {
            ...data,
            _theme: this.generateTheme(data), // El prefijo '_' indica metadatos de sistema
          };
        }
        return data;
      }),
    );
  }

  private generateTheme(niche: any): NicheTheme {
    // Aquí podrías traer los colores de la base de datos. 
    // Por ahora, devolvemos el esquema basado en el stock (Lógica de Bazar)
    const isBazar = niche.currentSkuCount < 25;

    return {
      colorPrimary: niche.primaryColor || '180 100% 50%', // Default: Cian
      colorAccent: niche.accentColor || '56 100% 50%',    // Default: Amarillo
      colorLight: '#ffffff',
      colorDark: '#0a0a0a',
      isBazar: isBazar,
    };
  }
}
Usa el código con precaución.
Paso 12: Registrar el Interceptor Globalmente
Para que no tengas que añadirlo manualmente en cada controlador, lo activamos en el punto de entrada del Oráculo.
Ubicación: apps/api-gateway/src/main.ts
typescript
import { ReactiveThemeInterceptor } from './app/common/interceptors/reactive-theme.interceptor';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  
  
  
  await app.listen(3000);
}

Usa el código con precaución.
Paso 13: El Pipe de Validación HSL
Como el administrador usará el Dashboard para cambiar colores, necesitamos asegurar que el formato H S% L% sea perfecto antes de guardarlo en MySQL.
Ubicación: apps/api-gateway/src/app/common/pipes/hsl-validator.pipe.ts
typescript
import { PipeTransform, Injectable, BadRequestException } from '@nestjs/common';

@Injectable()
export class HslValidatorPipe implements PipeTransform {
  transform(value: any) {
    const hslRegex = /^\d{1,3}\s\d{1,3}%\s\d{1,3}%$/;
    
    if (typeof value === 'string' && !hslRegex.test(value)) {
      throw new BadRequestException(
        `Falla de Portal: El color "${value}" no cumple el estándar HSL de ARCANI (H S% L%).`
      );
    }
    return value;
  }
}




Cómo debe quedar ahora:
Vamos a mover esta lógica al módulo de Theming que tienes en tu API Gateway, adaptándola a la interfaz NicheTheme que creamos en el Paso 1.
Ubicación: apps/api-gateway/src/app/modules/core-engine/theming/services/theming.service.ts
typescript
import { Injectable, Logger } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { NicheTheme } from '@arcani/shared/domain';
// Asegúrate de que esta entidad exista en tu data-access
import { NicheEntity } from '@arcani/data-access/db-entities'; 

@Injectable()
export class ThemingService {
  private readonly logger = new Logger(ThemingService.name);

  constructor(
    @InjectRepository(NicheEntity)
    private readonly nicheRepo: Repository<NicheEntity>,
  ) {}

  async resolveSintonia(slug: string): Promise<NicheTheme> {
    try {
      // 1. Buscamos el nicho y su configuración visual
      const niche = await this.nicheRepo.findOne({
        where: { slug: slug.toLowerCase() },
        select: ['primaryColor', 'accentColor', 'currentSkuCount'] // Optimizamos la consulta
      });

      // 2. Si no existe, aplicamos el ADN Maestro (Cian/Amarillo)
      if (!niche) {
        this.logger.warn(`Sintonía no encontrada para: ${slug}. Aplicando ADN Maestro.`);
        return this.getMasterTheme();
      }

      // 3. Regla del Bazar (Stock < 25)
      const isBazar = (niche.currentSkuCount ?? 0) < 25;

      return {
        colorPrimary: niche.primaryColor || '180 100% 50%',
        colorAccent: niche.accentColor || '56 100% 50%',
        colorLight: '#ffffff',
        colorDark: '#0a0a0a',
        isBazar: isBazar
      };

    } catch (error) {
      this.logger.error(`Falla de Portal en sintonía: ${error.message}`);
      return this.getMasterTheme();
    }
  }

  /**
   * ADN Maestro de ARCANI (Default)
   */
  private getMasterTheme(): NicheTheme {
    return {
      colorPrimary: '180 100% 50%', // Cian Core
      colorAccent: '56 100% 50%',   // Amarillo Volt
      colorLight: '#ffffff',
      colorDark: '#0a0a0a',
      isBazar: false
    };
  }
}





Así debe quedar tu controlador oficial dentro del módulo de Theming:
1. El Controlador de Sintonía Refactorizado
Ubicación: apps/api-gateway/src/app/modules/core-engine/theming/controllers/theming.controller.ts
typescript
import { Controller, Get, Query, BadRequestException } from '@nestjs/common';
import { ThemingService } from '../services/theming.service';
import { Public } from '../../../common/decorators/public.decorator';

@Controller('theming') // URL: /api/theming
export class ThemingController {
  constructor(private readonly themingService: ThemingService) {}

  /**
   * Sintoniza un portal basado en su slug.
   * Es @Public() porque el catálogo debe ser visible sin login.
   */
  @Public()
  @Get('tune') // URL final: /api/theming/tune?slug=anime
  async getSintonia(@Query('slug') slug: string) {
    if (!slug) {
      throw new BadRequestException('Falla de Portal: Se requiere un slug para sintonizar.');
    }

    // El servicio ahora consulta internamente el stock y el tema
    return await this.themingService.resolveSintonia(slug);
  }
}
Usa el código con precaución.
¿Qué mejoramos aquí?
Seguridad Total: Eliminamos el parámetro stock de la URL. Ahora el sistema es inviolable porque el ThemingService consulta el stock directamente de la tabla niche_stock en el servidor.
Ruta Semántica: Cambiamos /resolver por /tune (Sintonizar), alineándonos con el lenguaje del Ecosistema ARCANI.
Decorador Public: Usamos @Public() para permitir que los usuarios no logueados puedan ver los colores del nicho al navegar.





Para que el sistema de seguridad de NestJS sepa qué rutas son abiertas (como el catálogo) y cuáles requieren autenticación, necesitamos este decorador. En ARCANI, lo usamos para marcar las rutas que no necesitan el token de Firebase.
Aquí tienes la creación del archivo:
1. Crear el Decorador Public
Ubicación: apps/api-gateway/src/app/common/decorators/public.decorator.ts
typescript
import { SetMetadata } from '@nestjs/common';

/**
 * Metadata Key para identificar rutas que no requieren 
 * autenticación de Firebase (Google).
 */
export const IS_PUBLIC_KEY = 'isPublic';

/**
 * Decorador @Public()
 * Bypass para el AuthGuard. Úsalo en controladores o métodos 
 * que deben ser accesibles para cualquier explorador del portal.
 */
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);
Usa el código con precaución.
2. Por qué es crítico para el "Theming"
Tu ThemingController necesita ser público porque cuando un usuario entra por primera vez a ://arcani.com, el Frontend debe pedir los colores del nicho antes de que el usuario inicie sesión. Si no tienes este decorador, el FirebaseGuard bloqueará la petición y la web se quedará sin colores (o con el fallback).
3. Cómo lo usará tu Guard (Próximamente)
Cuando implementemos el FirebaseAuthGuard, este buscará la clave isPublic:
typescript
// Ejemplo de lógica en el Guard
const isPublic = this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [
  context.getHandler(),
  context.getClass(),
]);

if (isPublic) return true; // Si tiene @Public(), deja pasar la petición
Usa el código con precaución.






Para activar la Sintonía de Frecuencia en todo el ecosistema, debemos registrar el Interceptor y el Módulo de Theming en el corazón del Oráculo.
1. Registrar el Interceptor en el Módulo Principal
En NestJS, la mejor práctica para interceptores que dependen de otros servicios (como ThemingService) es registrarlos como proveedores con el token APP_INTERCEPTOR.
Ubicación: apps/api-gateway/src/app/app.module.ts
typescript
import { Module } from '@nestjs/core';
import { APP_INTERCEPTOR } from '@nestjs/core';
import { ReactiveThemeInterceptor } from './common/interceptors/reactive-theme.interceptor';
import { ThemingModule } from './modules/core-engine/theming/theming.module';
import { TypeOrmModule } from '@nestjs/typeorm';
// ... otras importaciones (Firebase, etc.)

@Module({
  imports: [
    // 1. Asegúrate de que el módulo de Theming esté importado
    ThemingModule,
    // ... tus otros módulos (Store, Users, etc.)
  ],
  providers: [
    // 2. Registro Global del Interceptor de Sintonía
    {
      provide: APP_INTERCEPTOR,
      useClass: ReactiveThemeInterceptor,
    },
  ],
})
export class AppModule {}
Usa el código con precaución.
2. Configurar el ThemingModule
Asegúrate de que el módulo esté exportando el servicio y cargando las entidades necesarias para que el interceptor pueda usarlas.
Ubicación: apps/api-gateway/src/app/modules/core-engine/theming/theming.module.ts
typescript
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { NicheEntity, NicheThemeEntity, NicheStockEntity } from '@arcani/data-access/db-entities';
import { ThemingController } from './controllers/theming.controller';
import { ThemingService } from './services/theming.service';

@Module({
  imports: [
    // Cargamos las 3 tablas necesarias para el ADN visual
    TypeOrmModule.forFeature([
      NicheEntity, 
      NicheThemeEntity, 
      NicheStockEntity
    ]),
  ],
  controllers: [ThemingController],
  providers: [ThemingService],
  exports: [ThemingService], // Crítico para que el Interceptor lo vea
})
export class ThemingModule {}
Usa el código con precaución.
¿Cómo verificar que todo funciona?
Levanta tu API (nx serve api-gateway).
Consulta un nicho (ej. GET http://localhost:3000/api/theming/tune?slug=anime).
Verifica la respuesta: Deberías ver el objeto del nicho y, al final, una propiedad llamada _theme con los colores HSL, el estado de Bazar y la geometría (borderRadius, glassBlur).



1. Crear el DTO de Respuesta (Shared Domain)
La mejor práctica es que la respuesta esté tipada y blindada en tu librería de dominio.
Ubicación: libs/shared/domain/src/lib/dtos/nichos/theming-response.dto.ts
typescript
import { Expose } from 'class-transformer';

export class ThemingResponseDto {
  @Expose()
  slug!: string;

  @Expose()
  portal: string = 'ARCANI_DYNAMICS';

  @Expose()
  timestamp: string = new Date().toISOString();

  // El Interceptor inyectará esto, así que también debemos exponerlo
  @Expose()
  _theme?: any; 

  constructor(partial: Partial<ThemingResponseDto>) {
    Object.assign(this, partial);
  }
}





# ==========================================================================================


¡Perfecto! Ahora vamos a cerrar el círculo. El Frontend debe estar atento a cualquier respuesta del Oráculo. Si la respuesta contiene un _theme, el Interceptor de Angular atrapará esa frecuencia y la enviará al motor para que toda la web cambie de color instantáneamente.
1. Crear el Interceptor de Sintonía (Angular)
Este archivo "escucha" el tráfico HTTP y actúa cuando detecta el ADN de ARCANI.
Ubicación: libs/core/reactive-engine/src/lib/interceptors/sintonia.interceptor.ts
typescript
import { HttpInterceptorFn, HttpResponse } from '@angular/common/http';
import { inject } from '@angular/core';
import { tap } from 'rxjs';
import { ThemeEngineService } from '../services/theme-engine.service';
import { NicheTheme } from '@arcani/shared/domain'; // Importamos tu interfaz

/**
 * Definimos una estructura local para que TypeScript 
 * reconozca el campo inyectado por el Oráculo.
 */
interface ArcaniResponse {
  _theme?: NicheTheme;
  [key: string]: any; // Permite cualquier otra propiedad (slug, portal, etc.)
}

export const sintoniaInterceptor: HttpInterceptorFn = (req, next) => {
  const themeEngine = inject(ThemeEngineService);

  return next(req).pipe(
    tap((event) => {
      // 1. Validamos que sea una respuesta HTTP con cuerpo
      if (event instanceof HttpResponse && event.body) {
        
        // 2. Casteamos el body a nuestra interfaz de respuesta ARCANI
        const body = event.body as ArcaniResponse;

        // 3. Ahora TypeScript ya no se queja: '_theme' es reconocido
        if (body._theme) {
          themeEngine.tune(body._theme);
        }
      }
    })
  );
};

Usa el código con precaución.
2. Registrar el Interceptor en la App
Para que Angular use esta función, debemos registrarla en la configuración de la aplicación (o en el app.config.ts de tu client-web).
Ubicación: apps/client-web/src/app/app.config.ts
typescript
import { provideHttpClient, withInterceptors } from '@angular/common/http';
import { sintoniaInterceptor } from '@arcani/core/reactive-engine';

export const appConfig: ApplicationConfig = {
  providers: [
    // Registramos el interceptor en el cliente HTTP
    provideHttpClient(
      withInterceptors([sintoniaInterceptor])
    ),
    // ... otros providers
  ],
};
Usa el código con precaución.
3. El Manifiesto (Exportación)
Asegúrate de que la librería core/reactive-engine exponga el interceptor para que sea visible desde apps/.
Ubicación: libs/core/reactive-engine/src/index.ts
typescript
export * from './lib/services/theme-engine.service';
export * from './lib/interceptors/sintonia.interceptor'; // Agrega esta línea
Usa el código con precaución.
¿Cómo probar que el sistema es autónomo?
Ahora no necesitas hacer nada manual. Simplemente haz una petición a tu nuevo endpoint desde cualquier componente de Angular:
typescript
// En cualquier componente:
this.http.get('http://localhost:3000/api/theming/tune?slug=anime').subscribe();
Usa el código con precaución.
Lo que sucederá:
Al completarse la suscripción, el Interceptor de Angular verá el _theme que enviamos desde NestJS.
Llamará a themeEngine.tune().
El Signal del motor se actualizará.
El Effect inyectará las variables CSS.
¡La web cambiará de color en milisegundos!