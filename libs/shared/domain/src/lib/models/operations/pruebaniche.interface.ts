/*
export interface Niche {
  uuid: string;
  slug: string;
  name: string;
  description?: string;
  cloudinary_id: string;

  // Sintonía HSL
  color_primary: string; // Formato "H S% L%"
  color_accent: string;
  color_light: string;
  color_dark: string;

  // Datos del Bazar (Añadir estos campos)
  current_sku_count: number; // 👈 Esto soluciona el error
  bazar_threshold: number;   // Para la lógica del computed()

  custom_config: {
    glassmorphism?: boolean;
    border_radius?: string;
    font_family?: string;
  };

  isActive: boolean;
}
 */
